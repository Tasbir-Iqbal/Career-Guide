# How to Start the Project

**Terminal 1** (Backend):
```bash
cd CareerGuideComp/backend
python -m uvicorn app.main:app --reload --port 8000
```

**Terminal 2** (Frontend):
```bash
cd CareerGuideComp
npm run dev
```
